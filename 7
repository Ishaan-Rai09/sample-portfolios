@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    @apply scroll-smooth;
  }
  body {
    @apply bg-navy text-slate-300 font-sans;
  }
}

@layer components {
  .glass-card {
    @apply bg-slate-800/50 backdrop-blur-lg border border-slate-700/50 rounded-2xl;
  }
  .gradient-text {
    @apply text-transparent bg-clip-text bg-gradient-to-r from-accent-cyan to-accent-blue;
  }
}
