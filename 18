import React from 'react';
import { motion } from 'framer-motion';
import { useInView } from 'react-intersection-observer';

const About: React.FC = () => {
  const { ref, inView } = useInView({ triggerOnce: true, threshold: 0.2 });

  return (
    <section id="about" className="py-24" ref={ref}>
      <motion.div
        initial={{ opacity: 0, y: 50 }}
        animate={inView ? { opacity: 1, y: 0 } : {}}
        transition={{ duration: 0.8 }}
      >
        <h2 className="text-4xl font-bold text-center mb-12">
          About <span className="gradient-text">Me</span>
        </h2>
        <div className="grid md:grid-cols-3 gap-12 items-center">
          <motion.div 
            className="md:col-span-1 flex justify-center"
            initial={{ opacity: 0, scale: 0.8 }}
            animate={inView ? { opacity: 1, scale: 1 } : {}}
            transition={{ duration: 0.8, delay: 0.2 }}
          >
            <div className="relative w-64 h-64">
              <div className="absolute inset-0 bg-gradient-to-br from-accent-blue to-accent-purple rounded-full blur-xl"></div>
              <img
                src="https://i.pravatar.cc/300?u=ishaanrai"
                alt="Ishaan Rai"
                className="relative w-full h-full object-cover rounded-full border-4 border-slate-700"
              />
            </div>
          </motion.div>
          <div className="md:col-span-2 text-lg text-slate-300 space-y-4">
            <p>
              Hello! I'm Ishaan, a passionate developer with a knack for creating dynamic and intuitive web applications. My journey into code started with a fascination for how things work, which quickly evolved into a full-blown passion for building elegant solutions to complex problems.
            </p>
            <p>
              With a foundation in both front-end and back-end technologies, I thrive in environments where I can bridge the gap between design and functionality. I'm driven by the challenge of crafting pixel-perfect UIs that are not only visually appealing but also highly performant and accessible.
            </p>
            <p>
              When I'm not coding, you can find me exploring new technologies, contributing to open-source projects, or enjoying a good cup of coffee.
            </p>
          </div>
        </div>
      </motion.div>
    </section>
  );
};

export default About;
